; BC250 ESP32 power switch.
; The setup portal's web UI lives in app/index.html and is packed into the
; SPIFFS partition. Flash it with:  pio run -t uploadfs

[platformio]
data_dir = app
default_envs = esp32-wroom-32

[env]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.13/platform-espressif32.zip
framework = arduino
; Custom table: ~2.75MB app (WiFi + BLE + async web server) + SPIFFS for the UI.
board_build.partitions = partitions.csv
board_build.filesystem = spiffs
monitor_speed = 115200
lib_deps =
	h2zero/NimBLE-Arduino@^2.2.3
	https://github.com/ESP32Async/ESPAsyncWebServer
	bblanchon/ArduinoJson@^7

[env:esp32-wroom-32]
board = esp32dev
board_upload.flash_size = 4MB
board_build.flash_mode = dio
build_flags =
	-D BC250_BOARD_ESP32_WROOM_32

[env:esp32-c3-devkitm-1]
board = esp32-c3-devkitm-1
build_flags =
	-D BC250_BOARD_ESP32_C3_DEVKITM_1
	-D BC250_NATIVE_USB_SERIAL=1
	-D ARDUINO_USB_MODE=1
	-D ARDUINO_USB_CDC_ON_BOOT=1
